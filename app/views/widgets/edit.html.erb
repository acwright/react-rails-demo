<h1>Editing Widget on Rails</h1>

<%= render 'form', widget: @widget %>

<%= link_to 'Show', @widget %> |
<%= link_to 'Back', widgets_path %>

<br><br>

<h1>Editing Widget on React</h1>

<%= react_component("WidgetsEdit", { widget: @widget, authenticity_token: form_authenticity_token }) %>
